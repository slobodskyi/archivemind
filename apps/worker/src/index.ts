/**
 * Worker entry point — the real skeleton lands in Phase 1 (issue #7):
 * ai_jobs claim loop (FOR UPDATE SKIP LOCKED), heartbeat, retry/backoff,
 * reaper, graceful shutdown, then handlers/ + services/ per TECH_SPEC §3, §7.
 */
console.log("archivemind worker: placeholder — skeleton lands in Phase 1 (issue #7)");
